# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'BuildHabit' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for BuildHabit
    pod 'Alamofire', '~> 4.7'
    pod 'SwiftyJSON', '~> 4.0'
    pod ‘Firebase/Core’
    pod 'AlamofireImage', '~> 3.3'

  target 'BuildHabitTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'BuildHabitUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
